import 'package:flutter/material.dart';

class SampahLiar extends StatelessWidget {
  const SampahLiar({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Sampah Liar'),
      ),
      body: const Center(
        child: Text('Sampah Liar Page'),
      ),
    );
  }
}
